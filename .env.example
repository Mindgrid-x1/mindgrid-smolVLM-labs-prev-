# REQUIRED: your Wi-Fi machine LAN IP (e.g. 192.41.1.211)
LAN_IP=192.41.1.211 ( MODIFY )

# HTTPS port for the site+proxy
PORT=8443

# llama.cpp server target (keep local HTTP; the Node proxy adds HTTPS)
API_TARGET=http://127.0.0.1:8080
